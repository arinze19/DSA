#include <iostream>
#include <map>
#include <vector>

std::vector<int> twoSum(std::vector<int> nums, int target)
{
    std::map<int, int> map;

    for (int index = 0; index < nums.size(); index++)
    {
    }
}

int main()
{
}